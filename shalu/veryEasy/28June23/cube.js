// Fix the code in the code tab to pass this challenge (only syntax errors). Look at the examples below to get an idea of what the function should do.
function cube (num){
    console.log(" before cube Number  ", num);
    let cubeNumber = num ** 3;
    console.log("after cube Number  ", cubeNumber);
    return  cubeNumber;
}
console.log(cube(5));