// Create a function that takes two numbers as arguments and returns their sum.
function addNumbers(a, b) {
    console.log(" before Add Numbers::", a,b);
    var sum ;
    sum = a + b;
    console.log("after add number::",sum);
}

addNumbers(3,5);
